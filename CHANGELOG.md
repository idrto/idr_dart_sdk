# Changelog

## 1.0.0

- Initial pub.dev release
- FFI bindings to `libidr_sdk` (`idr-ffi`)
- `IdrClient`: configure, auth, device join, agent start/stop, connect
- Pure Dart (no Flutter dependency)
- Flutter example app under `example/`
